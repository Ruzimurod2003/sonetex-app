@model AdministratorManufacturerToCatalog

<div class="text-center">
    <h1 class="text-dark">@Localizer["change_catalog"]</h1>
</div>

<hr />
<form asp-action="AddCatalog" method="post">
    <div class="grid grid-cols-1 gap-6 lg:grid-cols-2">
        <div class="panel">
            <div class="mb-5 flex items-center justify-between">
                <h5 class="text-lg font-semibold dark:text-white-light">@Model.ManufacturerName</h5>
            </div>
            <div class="mb-5">
                <div class="space-y-2">
                    <div>
                        <input type="hidden" asp-for="ManufacturerId" />
                        @foreach (Catalog catalog in Model.AllCatalogs)
                        {
                            <label class="inline-flex">
                                <input type="checkbox" class="form-checkbox text-info"
                                       name="ManufaturerIds" value="@catalog.Id"
                                @(Model.CatalogIds.Contains(catalog.Id) ? "checked=\"checked\"" : "") />
                                <span>@catalog.Name</span>
                            </label>
                        }
                    </div>
                    <div class="form-group flex">
                        <button type="submit" class="btn btn-success">
                            @Localizer["save"]
                        </button>
                        <a asp-action="Details" asp-route-id="@Model?.ManufacturerId" class="btn btn-info">
                            @Localizer["back_to_list"]
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>